# special-octo-guacamole

- add discord webhook before run

    ``settings`` -> ``Secrets and variables`` -> ``Actions`` -> ``New repository secret`` -> ``DISCORD_WEBHOOK``
    then add webhook link <br>
    save <br>
